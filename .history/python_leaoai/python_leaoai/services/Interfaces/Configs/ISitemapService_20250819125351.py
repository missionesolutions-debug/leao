from abc import ABC, abstractmethod
from typing import List, Optional, Any
from datetime import datetime
class ISitemapService(ABC):
    """
    Python interface equivalent to the C# ISitemapService.
    """
