from abc import ABC, abstractmethod
from typing import List, Optional, Any
from datetime import datetime
class IPageSectionItemService(ABC):
    """
    Python interface equivalent to the C# IPageSectionItemService.
    """
