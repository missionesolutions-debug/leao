$(document).ready(function () {

    if ($('.hdd-PrecoId').val() > 0) {

        var value = $('.hdd-PrecoId').val();

        if (value > 0) {
            $("#PrecoId").val(value);
            $("#PrecoId").trigger('change');
        }
    }

    if ($('.hdd-OrigemId').val() > 0) {

        var value = $('.hdd-OrigemId').val();

        if (value > 0) {
            $("#OrigemId").val(value);
            $("#OrigemId").trigger('change');
        }
    }

    if ($('.hdd-IndustriaId').val() > 0) {

        var value = $('.hdd-IndustriaId').val();

        if (value > 0) {
            $("#IndustriaId").val(value);
            $("#IndustriaId").trigger('change');
        }
    }

    if ($('.hdd-TipologiaId').val() > 0) {

        var value = $('.hdd-TipologiaId').val();

        if (value > 0) {
            $("#TipologiaId").val(value);
            $("#TipologiaId").trigger('change');
        }
    }

    if ($('.hdd-CorId').val() > 0) {

        var value = $('.hdd-CorId').val();

        if (value > 0) {
            $("#CorId").val(value);
            $("#CorId").trigger('change');
        }
    }

    if ($('.hdd-ClienteId').val() > 0) {
        
        var value = $('.hdd-ClienteId').val();

        if (value > 0) {
            $("#ClienteId").val(value);
            $("#ClienteId").trigger('change');
        }
    }


    if ($('.hdd-UsuarioId').val() > 0) {
        var value = $('.hdd-UsuarioId').val();

        if (value > 0) {
            $("#UsuarioId").val(value);
            $("#UsuarioId").trigger('change');
        }

    }

    if ($('.hdd-AreaAtuacaoId').val() > 0) {
        var value = $('.hdd-AreaAtuacaoId').val();

        if (value > 0) {
            $("#AreaAtuacaoId").val(value);
            $("#AreaAtuacaoId").trigger('change');
        }

    }

    if ($('.hdd-ServicoId').val() > 0) {
        var value = $('.hdd-ServicoId').val();

        if (value > 0) {
            $("#ServicoId").val(value);
            $("#ServicoId").trigger('change');
        }

    }

    if ($('.hdd-EquipeId').val() > 0) {
        var value = $('.hdd-EquipeId').val();

        if (value > 0) {
            $("#EquipeId").val(value);
            $("#EquipeId").trigger('change');
        }

    }

    if ($('.hdd-ProdutoId').val() > 0) {
        var value = $('.hdd-ProdutoId').val();

        if (value > 0) {
            $("#ProdutoId").val(value);
            $("#ProdutoId").trigger('change');
        }

    }

    if ($('.hdd-TipoFluxoCaixaId').val() > 0) {
        var value = $('.hdd-TipoFluxoCaixaId').val();

        if (value > 0) {
            $("#TipoFluxoCaixaId").val(value);
            $("#TipoFluxoCaixaId").trigger('change');
        }

    }


    if ($('.hdd-ProjectId').val() > 0) {
        
        var value = $('.hdd-ProjectId').val();

        if (value > 0) {
            $("#ProjectId").val(value);
            $("#ProjectId").trigger('change');
        }

    }

    if ($('.hdd-ProjectStatusId').val() > 0) {
        
        var value = $('.hdd-ProjectStatusId').val();

        if (value > 0) {
            $("#ProjectStatusId").val(value);
            $("#ProjectStatusId").trigger('change');
        }

    }

    if ($('.hdd-JobTaskId').val() > 0) {
        
        var value = $('.hdd-JobTaskId').val();

        if (value > 0) {
            $("#JobTaskId").val(value);
            $("#JobTaskId").trigger('change');
        }

    }

    if ($('.hdd-JobTaskStatusId').val() > 0) {
        
        var value = $('.hdd-JobTaskStatusId').val();

        if (value > 0) {
            $("#JobTaskStatusId").val(value);
            $("#JobTaskStatusId").trigger('change');
        }

    }

    if ($('.hdd-MenuId').val() > 0) {
        
        var value = $('.hdd-MenuId').val();

        if (value > 0) {
            $("#MenuId").val(value);
            $("#MenuId").trigger('change');
        }

    }


    if ($('.hdd-TipoCategoriaId').val() > 0) {
        
        var value = $('.hdd-TipoCategoriaId').val();

        if (value > 0) {
            $("#TipoCategoriaId").val(value);
            $("#TipoCategoriaId").trigger('change');
        }

    }


    if ($('.hdd-CategoriaId').val() > 0) {
        
        var value = $('.hdd-CategoriaId').val();

        if (value > 0) {
            $("#CategoriaId").val(value);
            $("#CategoriaId").trigger('change');
        }

    }


    if ($('.hdd-SubCategoriaId').val() > 0) {
        
        var value = $('.hdd-SubCategoriaId').val();

        if (value > 0) {
            $("#SubCategoriaId").val(value);
            $("#SubCategoriaId").trigger('change');
        }

    }

    if ($('.hdd-HostServerId').val() > 0) {
        
        var value = $('.hdd-HostServerId').val();

        if (value > 0) {
            $("#HostServerId").val(value);
            $("#HostServerId").trigger('change');
        }
    }

    if ($('.hdd-PaginaId').val() > 0) {
        
        var value = $('.hdd-PaginaId').val();

        if (value > 0) {
            $("#PaginaId").val(value);
            $("#PaginaId").trigger('change');
        }
    }
});