from abc import ABC, abstractmethod
from typing import List, Optional, Any
from datetime import datetime
class IAccountService(ABC):
    """
    Python interface equivalent to the C# IAccountService.
    """
