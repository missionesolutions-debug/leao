from abc import ABC, abstractmethod
from typing import List, Optional, Any
from datetime import datetime
class IAuthenticationService(ABC):
    """
    Python interface equivalent to the C# IAuthenticationService.
    """
